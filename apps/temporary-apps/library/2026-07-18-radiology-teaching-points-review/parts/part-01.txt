<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#0b1020">
  <title>Radiology Teaching Points Review</title>
  <style>
    :root {
      color-scheme: dark;
      --bg: #090d18;
      --panel: #111827;
      --panel-2: #172033;
      --line: #26334f;
      --text: #edf2ff;
      --muted: #9eabc5;
      --accent: #9b8cff;
      --accent-2: #63d7ff;
      --fav: #ffd166;
      --danger: #ff7a90;
      --shadow: 0 14px 40px rgba(0,0,0,.28);
      --radius: 18px;
    }

    * { box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      min-height: 100vh;
      background:
        radial-gradient(circle at 15% -10%, rgba(155,140,255,.20), transparent 34rem),
        radial-gradient(circle at 100% 0%, rgba(99,215,255,.12), transparent 30rem),
        var(--bg);
      color: var(--text);
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      line-height: 1.45;
    }

    button, input { font: inherit; }

    .app {
      width: min(1100px, 100%);
      margin: 0 auto;
      padding: 0 16px 70px;
    }

    .hero {
      padding: 34px 4px 20px;
    }

    .eyebrow {
      color: var(--accent-2);
      font-size: .78rem;
      font-weight: 800;
      letter-spacing: .13em;
      text-transform: uppercase;
    }

    h1 {
      margin: 8px 0 8px;
      font-size: clamp(2rem, 6vw, 3.4rem);
      line-height: 1.02;
      letter-spacing: -.045em;
    }

    .subtitle {
      max-width: 760px;
      margin: 0;
      color: var(--muted);
      font-size: 1rem;
    }

    .toolbar-wrap {
      position: sticky;
      top: 0;
      z-index: 20;
      padding: 10px 0 12px;
      background: linear-gradient(to bottom, rgba(9,13,24,.98) 72%, rgba(9,13,24,0));
      backdrop-filter: blur(12px);
    }

    .toolbar {
      display: grid;
      grid-template-columns: minmax(0,1fr) auto;
      gap: 10px;
      padding: 12px;
      border: 1px solid rgba(255,255,255,.08);
      border-radius: var(--radius);
      background: rgba(17,24,39,.92);
      box-shadow: var(--shadow);
    }

    .search {
      width: 100%;
      min-height: 44px;
      border: 1px solid var(--line);
      border-radius: 13px;
      background: #0c1323;
      color: var(--text);
      padding: 0 14px;
      outline: none;
    }

    .search:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(155,140,255,.16);
    }

    .action-row {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .btn {
      min-height: 44px;
      border: 1px solid var(--line);
      border-radius: 13px;
      padding: 0 13px;
      background: #131c2e;
      color: var(--text);
      cursor: pointer;
      transition: .16s ease;
      white-space: nowrap;
    }

    .btn:hover { transform: translateY(-1px); border-color: #47587b; }
    .btn:active { transform: translateY(0) scale(.98); }

    .btn.active {
      background: linear-gradient(135deg, rgba(155,140,255,.25), rgba(99,215,255,.15));
      border-color: var(--accent);
    }

    .btn.primary {
      background: linear-gradient(135deg, #725dff, #4cbfe8);
      border-color: transparent;
      color: white;
      font-weight: 800;
    }

    .meta-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      padding: 10px 4px 0;
      color: var(--muted);
      font-size: .88rem;
    }

    .section {
      margin-top: 18px;
      border: 1px solid rgba(255,255,255,.07);
      border-radius: var(--radius);
      background: rgba(17,24,39,.74);
      overflow: clip;
      box-shadow: 0 10px 30px rgba(0,0,0,.16);
    }

    .section-title {
      margin: 0;
      padding: 20px 20px 10px;
      font-size: 1.28rem;
      line-height: 1.2;
      letter-spacing: -.02em;
    }

    .subsection-title {
      margin: 0;
      padding: 16px 20px 8px;
      color: #c8d2eb;
      font-size: 1rem;
      border-top: 1px solid rgba(255,255,255,.05);
    }

    .items {
      padding: 4px 10px 14px;
    }

    .item {
      width: 100%;
      display: grid;
      grid-template-columns: 28px minmax(0, 1fr);
      gap: 10px;
      align-items: start;
      margin: 5px 0;
      padding: 12px 12px;
      border: 1px solid transparent;
      border-radius: 14px;
      background: transparent;
      color: var(--text);
      text-align: left;
      cursor: pointer;
      transition: background .15s ease, border-color .15s ease, transform .15s ease;
      -webkit-tap-highlight-color: transparent;
    }

    .item:hover {
      background: rgba(255,255,255,.035);
      border-color: rgba(255,255,255,.06);
    }

    .item:active { transform: scale(.995); }

    .item.favorite {
      background: linear-gradient(135deg, rgba(255,209,102,.12), rgba(155,140,255,.09));
      border-color: rgba(255,209,102,.38);
    }

    .star {
      width: 24px;
      height: 24px;
      display: grid;
      place-items: center;
      color: #65718c;
      font-size: 1.18rem;
      line-height: 1;
      transition: .15s ease;
    }

    .item.favorite .star {
      color: var(--fav);
      transform: scale(1.08);
      text-shadow: 0 0 18px rgba(255,209,102,.35);
    }

    .item-copy {
      min-width: 0;
      overflow-wrap: anywhere;
    }

    .item-copy strong {
      color: white;
    }

    .depth-1 .item-copy { padding-left: 14px; }
    .depth-2 .item-copy { padding-left: 30px; color: #d2daeb; }
    .depth-3 .item-copy { padding-left: 46px; color: #bec9dd; }

    mark {
      background: rgba(99,215,255,.24);
      color: inherit;
      border-radius: 4px;
      padding: 0 .08em;
    }

    .empty {
      display: none;
      margin: 30px 0;
      padding: 34px 20px;
      text-align: center;
      color: var(--muted);
      border: 1px dashed var(--line);
      border-radius: var(--radius);
      background: rgba(17,24,39,.55);
    }

    .toast {
      position: fixed;
      left: 50%;
      bottom: calc(20px + env(safe-area-inset-bottom));
      transform: translate(-50%, 18px);
      z-index: 50;
      padding: 11px 15px;
      border: 1px solid rgba(255,255,255,.1);
      border-radius: 999px;
      background: rgba(17,24,39,.96);
      color: white;
      box-shadow: var(--shadow);
      opacity: 0;
      pointer-events: none;
      transition: .2s ease;
