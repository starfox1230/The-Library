exclude:
  # Anki cloze syntax (for example {{c1::answer}}) is not Liquid markup.
  # Keep the apps publishable, but do not ask Jekyll to render their source notes.
  - "apps/anki-card-creation-codex-helper/**/*.md"
  - "apps/anki-pocket-knife/*.md"
